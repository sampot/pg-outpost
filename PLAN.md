# 離島前哨（`pg-outpost`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-outpost.yaml`

## 1. 一句話

派五名殖民者輪班工作、餵飽他們的三項需求，沿營地→倉庫→工坊→信標塔蓋出生產鏈，撐到第 18 日點亮信標的殖民經營小品；玩法為原創設計，非任一商業作品復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-outpost` / `game` / `策略` |
| status | `unlisted`（待上架驗收） |
| 模式 | 單人回合制日結經營；無 AI 對手（事件即對手） |
| 勝負 | 信標塔建成＋第 18 日後＋士氣 ≥38 即勝；在職 ≤1 人／連續 2 日斷糧／士氣 ≤8 連續 2 日則敗 |
| 人口 | 固定 5 名具名殖民者（阿海/小晴/老周/美玲/阿東）；離島不復返 |
| 生產鏈 | 6 工作站 × 4 建築線性解鎖（倉庫 12 木→工坊 18 木→信標 25 木+10 零件） |
| 隨機性 | `seeded()` 雜湊；事件僅在第 4/8/12… 日以 `seeded(seed, day, 17)` 決定性觸發 |
| 素材 | Kenney 圖示＋Kenney 音效＋BlippyBits 音樂 `assets/`（CC0，仍署名） |
| 交付形 | 純 HTML＋CSS＋ESM JS；無 build；`npx vitest run` 測試 |

## 3. 完整規則（現行實作）

### 3.1 回合流程與勝負

- 操作循環：點殖民者 → 點工作站派工（可重派/取消）→「建造下一階」（可）→「結束這一天」。
- 日結順序固定：生產結算 → 吃飯 → 被動衰減 → 離島判定 → 事件 → 日數+1 → 勝負檢查。
- 勝利加分手動：達成時分數 `+120 + 士氣`；建築本身給 `25 + chain×10` 分（35/45/55）；日常生產計分＝木 1／食物 2／零件 4／餐點 3 每單位。

### 3.2 殖民者需求曲線（實際值）

- 三需求初始：飽足 72／休息 68／社交 60；個人心情＝三項平均（0–100），離島者記 0。
- 殖民地士氣＝在職者心情平均 ＋ `min(20, 餐點存量×2) × 0.35`，上限 100——餐點是唯一全局士氣來源。
- 心情產量倍率：≥70 → **1.25**；≥45 → 1.0；≥25 → **0.65**；<25 → **0.35**。低心情直接砍產能。

### 3.3 工作站表（`STATIONS`）

| 站 | 產出(base) | 前置建築 | 成本/日 | 消耗(需求/日) | 回復 |
| --- | --- | --- | --- | --- | --- |
| 伐木 forage | 木材 3 | — | — | 休息−10 飽足−5 社交−2 | — |
| 農耕 farm | 食物 4 | — | — | 休息−12 飽足−6 社交−2 | — |
| 炊事 kitchen | 餐點 2 | 倉庫 | 食物 2 | 休息−8 飽足−4 社交−1 | — |
| 工匠 craft | 零件 2 | 工坊 | 木材 3 | 休息−14 飽足−5 社交−3 | — |
| 聚會 social | 士氣 6 | — | — | 飽足−4 休息−4 | 全員社交+round(總量/4)；本人社交+14 |
| 休息 rest | — | — | — | 飽足−3 社交−1 | 本人休息+20 |

- 有成本的站在資源不足時當日**靜默罷工並清空該職**（job=null），玩家須注意資源鏈。
- 被動衰減：每日社交 −3（聚會中者免）、無業或休息者再 −2 休息。

### 3.4 吃飯與饑荒

- 每日在職者各吃 1 食物；餐點優先抵扣（有餐點時全員飽足 +8，即使只部分覆蓋），不足處由食物補（補到者飽足 +4）。
- 斷糧：食物歸 0、饑荒計數 +1、全員飽足 −18／休息 −6；饑荒計數滿足當日吃飽即歸零。

### 3.5 離島與敗北

- 心情 ≤18 或飽足 ≤8 → 次日結算時離島（log 記錄「士氣崩潰」），不可逆。
- 敗北：在職人數 ≤1；或 `famineDays ≥ 2`；或殖民地士氣 ≤8 連續 2 日（中間回升即重置計數）。

### 3.6 事件（每 4 日一次）

- 以 `seeded(seed, day, 17)` 抽籤：<0.28 暴風雨（無倉庫 −5 糧／有倉庫 −2 糧）、<0.5 漂木（木 +6）、<0.72 想家（全員社交 −12）、其餘補給船（糧 +4、全員社交 +6）。同 seed 同序列，可測。

### 3.7 邊界處理

- 全狀態 `structuredClone` 不可變更新；未選人就點工作站以 toast 提示（不用原生對話框）。
- 對已離島者選取顯示「這位殖民者已離島」；鎖定工作站派工被拒並提示「尚未解鎖」。
- 舊字串 action API（`applyAction`："build"/"nextDay"/"assign:x"/"farm"/"lumber"）保留供測試與簡化入口。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 點殖民者卡 | 選取（顯示三需求條與取消派工鈕） |
| 點工作站 | 把選中人派去該站（顯示現有人數） |
| 建造下一階 | 資源足夠才可按；沿鏈推進 |
| 結束這一天 | 推進日結算 |
| 音效鈕 | 開/關（僅記憶體，未持久化） |
| 再守一季 | 終局 overlay 直接換 seed 重開 |

- HUD：日數、士氣 pill（<30 變色）、分數、最佳分、四資源列；建造鏈步條（✓/編號/成本）；終局 role=dialog overlay。
- Mobile-first 單欄；toast 取代 alert；`visibilitychange`/`pagehide` 自動暫停音訊。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| `outpost:progress`（KV，經 `globalThis.PG.kv`） | `{best, wins, plays, updatedAt, last:{score,outcome,day}}` | boot 載入；終局後 PUT |
| （無）localStorage | — | 本 repo 未用 localStorage；音效開關僅記憶體 |

- key 已帶 `outpost:` 前綴，符合命名空間慣例。KV 讀失敗 toast「讀取戰績失敗；仍可遊玩」，寫失敗 toast 不阻遊玩。
- 無自訂 functions.js；一切經宿主注入 `globalThis.PG.kv.get/put`（boot 等 `PG.ready`）。

## 6. 美術／音效／署名

- `assets/icons/` 7 枚（wood/wheat/apple/planks/hearts/house/colonist.png）— Kenney Board Game Icons（CC0），UI 直接引用。
- 音效 click/assign/build/day/event/win/lose.ogg — Kenney Interface Sounds／RPG Audio（CC0）；BGM `music.ogg` — BlippyBits「Dew」（Dylann Taylor）。SFX 預設音量 0.55、BGM loop 0.22。
- `assets/images/` die/emblem/hero/rival.png 已入庫與 manifest 但**未被程式引用**（同 villagewrath 的閒置素材包）。
- 詳 `ATTRIBUTION.md` 與 `assets/licenses/*.txt`——CC0 也照專案慣例署名。新增素材一律拷進 `assets/`、更新 ATTRIBUTION、同步 `sam-manifest.json` files。

## 7. 測試（`npx vitest run`）

現有覆蓋（`game.test.js`，約 30 例）：初始盤面與決定論、心情公式與離島零分、殖民地士氣聚合、基礎站解鎖前不可派（kitchen/craft）、派工/取消不可變、鎖定站拒絕、canBuild 資源閘、倉庫→工坊→信標逐階花費、雙伐木增產、每日進食扣糧、斷糧饑荒計數、炊事轉餐點、工匠耗木產零件、seeded 可重放且隨 seed 變、暴風雨倉庫保護差額、漂木 +6 木、在職 ≤1 敗、饑荒敗、信標+日期+士氣勝利、無信標不早勝、低心情離島、legal actions、summarize 形狀、applyAction 路由與非法 action 安全忽略、mergeProgress 最佳分/勝場/場次。

UI（app.js DOM）與音訊不在測試範圍；改需求曲線／倍率／門檻必補邊界測試。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM）；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；提示一律 toast／overlay。
3. Mobile-first；主操作不可 hover-only。
4. 戰績以 `globalThis.PG.kv` 為唯一權威（key 前綴 `outpost:`）；禁止裸 localStorage 當權威。
5. 不自行載入 `sdk.js`；宿主注入 `window.PG`（等 `PG.ready`，standalone 也要能玩）。
6. 遊戲邏輯保持純函式＋不可變更新；隨機一律走 `seeded()` 種子流，禁 `Math.random` 於模擬內。
7. 改動可執行邏輯前先寫失敗測試（TDD）。
8. 檔案清單變動須同步 `sam-manifest.json`（下載契約）。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化「人力調度與需求平衡」的深度，不改變「派工—日結—生產鏈」的核心認同。

**高優先**

1. **多人在同一站的收益遞增／分工加成**：目前每人是獨立乘區，多人同站沒有協作感。加簡單規則（如同站第 2 人起 base ×1.15，或聚會站人數越多回復越高）即可讓「排班」出現取捨；改 resolveProduction 一處。
2. **事件應對互動**：暴風雨/想家目前純懲罰通知。加入 1 回合應對選項（「搶收：犧牲明日產能保住糧」「辦活動：耗 2 餐點抵銷想家」），把事件從被動挨打變決策點。
3. **難度/目標曲線**：TARGET_DAY 18 固定且事件溫和，通關路線單一。提供「寧靜/標準/風浪」三檔（調事件機率與 upkeep），或縮短至 12 日的速通模式。

**中優先**

4. **殖民者個性**：五人目前數值完全相同。給每人 ±20% 的需求傾斜（老周吃得快、小晴社交需求高），搭配頭像差異化，讓「派誰」也有學問。
5. **餐點定位強化**：餐點已是唯一全局士氣來源但取得昂貴（食物 2 換 1）；把 colonyMorale 公式中餐點項顯示在 HUD（「餐點士氣 +x.x」），讓玩家看見投資報酬。
6. **里程碑與稱號**：以 progress.last 延伸存「最快通關日數」「零離島通關」，開場畫顯示徽章，增加重玩目標。

**低優先**

7. **接線/清理閒置素材**：assets/images 四張圖未使用；用於終局畫或移除出 manifest。
8. **音效開關持久化**：仿空氣曲棍球記住偏好；error.ogg 已入庫未接線，可用於「資源不足建造」提示音。
